# 群文游文字世界 AstrBot 插件

这是一个学院都市题材的小时制群文游系统，包含 AstrBot 群插件、SQLite 数据库和内置前端后台。

## 已实现的 v1/MVP 功能

- 群内开启世界
- 玩家提交角色卡
- 管理员前端审核/编辑角色卡并绑定 QQ 号
- 每小时提交一次行动
- 每小时统一结算
- 大模型一次性输出群公告和所有玩家私聊结果，程序拆分发送
- 大模型失败时使用程序模板兜底
- 每两小时公共事件
- 管理员前端预设事件，并加入下一轮结算
- 地图移动约束，防止瞬移
- NPC 随机行动
- 玩家/NPC/玩家相遇判断
- 学都币、水分、饱食度、生命、精力、心情
- 背包和基础商店框架
- 管理员前端添加商品
- 玩家前端查看自己的角色、状态、可见历史和商店
- 每日签到加学都币
- 每天早上 8 点私聊状态栏
- 玩家和 NPC、玩家和玩家之间的好感度基础波动
- 世界历史查询

## 常用群命令

```text
世界帮助
世界开启
创建角色 游戏名 | 身份设定
行动 下一步要做什么
状态
地图
待结算
签到
绑定文游私聊
世界结算
世界事件
世界后台
```

## 前端后台

默认地址：

```text
http://127.0.0.1:8787
```

默认管理员账号：

```text
admin
```

默认管理员密码：

```text
admin123456
```

上线前请在 AstrBot 插件配置里修改管理员密码。

玩家前端账号会在提交角色卡或管理员创建角色时生成：账号为游戏名，初始密码默认是 QQ 后 6 位。管理员在角色表单里填写“初始/重置密码”时，会重置该玩家账号密码。

## 数据位置

默认数据库：

```text
data/plugin_data/astrbot_plugin_text_world/text_world.sqlite3
```

## 推荐使用流程

1. 群里发送 `世界开启`。
2. 玩家发送 `创建角色 游戏名 | 身份设定`。
3. 管理员打开前端后台审核角色卡，补充阵营、能力、战斗力。
4. 玩家私聊 bot 发送 `绑定文游私聊`。
5. 玩家每小时在群里发送 `行动 内容`。
6. 系统定时结算并私聊个人结果，群里发送公共公告。

## 设计边界

程序负责硬规则：地图、状态、货币、背包、每小时一次行动、反作弊和数据库。

大模型负责叙事：群公告和个人结果文本润色。模型不能直接决定最终状态，避免 AI 随意改世界。配置里可以关闭 `enable_llm` 使用纯程序模板，也可以只关闭 `enable_ai_private_result`，让私聊结果固定使用程序权威模板、仅群公告走大模型。

## 大模型消耗说明

每轮结算最多调用一次大模型：程序把本轮行动、公共事件、NPC 动向和个人结果打包成一个 JSON 请求，模型一次性返回群公告和各玩家私聊文本，插件再拆开发送。没有到结算周期不会持续调用；关闭大模型后不产生模型调用。
