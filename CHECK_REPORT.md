# AstrBot 群文游文字世界插件十轮检查报告

## 检查范围

- 插件入口与定时任务：`main.py`
- SQLite 迁移、账号、会话和默认数据：`core/database.py`
- 角色、行动、结算、事件、商店、背包、好感度：`core/service_v2.py`
- 大模型一次输出拆分与兜底：`core/narrator_v2.py`
- 内置 Web 前端和 API：`core/webapp.py`
- 配置与说明：`_conf_schema.json`、`README.md`

## 运行证据

- `python -m compileall .`：通过。
- 临时 SQLite 端到端模拟：通过。
- 旧版 SQLite 表结构迁移模拟：通过。
- 内置前端 HTML 脚本解析：通过。
- 不生成 `__pycache__` 的 Python 源码编译检查：通过。
- 模拟覆盖：建世界、创建/审核角色、后台编辑角色保留旧状态、非法审核状态/地点拒绝、跨 QQ 同名账号拒绝、管理员账号不可作为玩家账号、商品效果校验、限量商品购买扣库存并入背包、事件预设可重复触发、玩家前端数据范围限制。
- 追加复查覆盖：后台角色表单留空学都币不覆盖旧余额、新角色使用配置初始货币、管理员显式重置玩家密码、商品名直接出现在行动文本中也可购买、Web API 坏 JSON 走统一错误处理。
- 本机未安装 AstrBot 包，因此没有直接 import `main.py` 启动真实 AstrBot 运行时；已对核心模块做本地回归。

## 十轮结论

1. 角色保存与账号边界：修复后台编辑已有角色时把生命/水分/饱食度等重置为默认值的问题；新增审核状态和地点 key 校验。
2. 结算状态与事件：修复预设事件使用一次后无法再次点击触发的问题。
3. 商店与背包：新增商品效果 JSON 对象校验、库存下限保护、购买时脏效果兜底；后台显式填写密码时会真正重置玩家密码。
4. 前端权限与数据范围：修复玩家输入内容直接 `innerHTML` 渲染的 XSS 风险；新增玩家可见的“我的角色”和“商店”区域。
5. 定时任务与发送：结算返回群公告和私聊发送成功率，私聊失败不会中断整轮。
6. 大模型输出：`enable_ai_private_result` 开关现在生效；JSON 解析支持代码块包裹并保留模板兜底。
7. 数据库迁移：补充缺列迁移和高频查询索引，降低旧库升级失败和查询变慢风险。
8. 配置与文档：同步新增开关、玩家账号/重置密码说明和大模型消耗说明。
9. 端到端回归：核心业务链路通过临时库模拟。
10. 最终清理：编译通过，待清理 `__pycache__` 后交付。

## 追加复查结论

- 修复后台角色表单默认学都币导致审核时误覆盖余额的问题，改为留空不修改。
- 修复 Web API 解析坏 JSON 时可能绕过统一错误响应的问题。
- 增强购买识别：行动文本包含现有商品名，或包含 `buy`，也会进入购买流程。
- 旧库迁移、前端脚本解析、增强业务回归均通过。

## 再次十轮自查结论

### 为什么多轮检查仍会发现问题

这个插件本质上是一个小型文字游戏服务器，而不是单个命令脚本。它同时包含账号系统、Web 前端、SQLite 迁移、群命令、定时结算、大模型润色、商店背包和事件效果。前几轮主要查功能正确性，后续切到“安全”“旧库升级”“运营误操作”“坏输入”“并发结算”等视角时，会照出不同类别的问题。单靠人工阅读很难一次覆盖所有交叉组合，因此这次补了更多可重复模拟。

### 本轮新增修复

- 配置布尔值支持字符串 `false`、`0`、`no`、`off` 等写法，避免配置界面传字符串时被 Python `bool("false")` 误判为启用。
- 管理员重置玩家密码时要求至少 6 位，并清理该玩家旧 session。
- Web API 限制请求体大小，拒绝非对象 JSON，事件触发 id 做明确校验。
- 空 QQ 号不能创建角色、绑定私聊或提交行动。
- 生命值为 0 的角色只能休息或治疗，不能继续普通行动。
- 手动结算和定时结算增加同群世界锁，避免重复推进同一轮。
- 事件效果必须是 JSON 对象。
- 旧库迁移时给历史角色回填玩家前端账号。
- 大模型返回文本增加长度裁剪，避免异常长输出刷屏。
- 治疗/休息/购买识别补充英文关键词和商品名触发。

### 本轮新增验证

- 配置字符串布尔值解析：通过。
- 低生命行动限制和治疗例外：通过。
- 重置密码后旧 session 失效：通过。
- 大模型超长输出裁剪：通过。
- 旧库角色账号回填：通过。
- Python 源码编译：通过。
- 前端脚本解析：通过。

## 不变量逻辑复查结论

这轮改用“不变量”而不是“模块”来查：身份不能串、轮次不能重复、资源不能异常、玩家不能看后台隐藏数据、旧库升级不能破坏账号、大模型不能新增私聊对象。

### 本轮新增修复

- 未审核角色不能签到拿学都币。
- 玩家前端商品数据不再返回 `effect_json`，避免泄露后台隐藏效果。
- 触发不存在的预设事件会明确报错，不再静默成功。
- 旧库迁移回填账号时，跨群同名不同 QQ 不会串到同一玩家账号；和管理员账号重名的旧角色会自动追加 QQ 后缀。
- 旧角色账号回填顺序调整为管理员账号创建之后，避免抢占管理员账号名。

### 本轮新增验证

- 轮次推进不变量：连续结算只按轮次递增，不重复处理已结算行动。
- 玩家数据隔离：玩家快照只能看到自己角色、公开历史和自己的私密历史。
- 资源边界：坏价格/库存输入不会导致负库存或崩溃。
- 旧库迁移：同名不同 QQ、角色名撞管理员账号均可安全回填。
- 大模型污染：模型返回额外 QQ key 时不会新增私聊对象。
- Python 源码编译和前端脚本解析均通过。

## 剩余建议

- 真机 AstrBot 环境里再做一次群聊/私聊实测，重点看不同平台的 `unified_msg_origin` 和 OneBot 私聊兜底。
- 后续若客户继续扩展地图编辑、NPC 记忆编辑、玩家网页购物，建议补操作审计日志和管理员多账号管理。

## 2026-05-11 客户反馈：移动、地点、吃喝、NPC 交互

### 检查范围

- 每回合移动限制：默认改为 3 格，可通过 `max_move_steps` 调整。
- 行动内容限制：行动文本上限提升到 3000 字，移动只限制地图位移，不限制行动内容描述。
- 地点识别：第七学区入口/门口与某高中校门分离；地点支持别名和模糊搜索。
- 生活消费：食物、饮水、拉面、套餐按日常价格处理，吃喝恢复大幅提高。
- NPC 交互：医院医生等岗位型 NPC 不再被随机移动导致找不到。

### 本轮新增修复

- 修复模糊地点命中后又被“邻近地点优先”排序抢走的问题，例如“第十学区少年院”不会再误落到第十二学区。
- 修复“第7学区入口/路口”与“第七学区某高中校门”混淆，只有明确高中/学校/校门语义才识别成 `school_gate`。
- 购买逻辑改为明确商品和类别需求合并处理：`拉面+水`、`食物+水` 会同时购买；已买到食物/水后不会重复加购。
- 修复水类关键词误匹配“防水学生包”的问题，分类购买现在看商品效果字段：饱食恢复才算食物，水分恢复才算饮水。
- 旧数据库默认商品会温和升级旧效果：矿泉水、便当、拉面、套餐等恢复量更新到新版；后台自定义过效果的商品不强行覆盖。
- 医生、医疗机构、教师、警备员等岗位型 NPC 默认固定，不参与每轮随机游走，避免“去医院找冥土追魂”结算时医生随机离岗。
- 系统提示加入学都币购买力和学生日收入标尺，叙事模型不会把基础吃喝写得昂贵或难以获得。

### 本轮验证

- 临时 SQLite 端到端模拟：通过。
- 覆盖用例：
  - 校门直接去 `Seventh Mist` 买饮用水和便当，3 格内可到达，扣 28 学都币，水分/饱食接近满值。
  - `7学区路口` 模糊识别到第七学区中心，不误识别为高中校门。
  - 主干道吃拉面再买水，扣 34 学都币，水分/饱食恢复到可游玩状态。
  - 医院找冥土追魂请教治疗，私聊结果包含 NPC 互动反馈。
  - 校门直达第十学区少年院超过 3 格，被地图规则拦截并留在原地。

## 2026-05-11 客户反馈：世界时间联通现实时间

### 本轮新增修复

- 新增统一时间上下文 `time_context`，世界时间直接取现实北京时间 `Asia/Shanghai`。
- 状态栏、待结算、群公告、私聊个人结果都会显示当前现实联动时间。
- 每轮结算传给大模型的 JSON 中包含 `time_context`，并在旁白规则里要求按现实日期、星期和时段描写昼夜、上课/放学、营业、人流与交通。
- WebUI 总览和世界卡片显示当前世界时间，方便管理员确认剧情时段。

### 本轮验证

- 临时 SQLite 结算模拟：群公告、私聊结果和结算返回结构均包含 `time_context`。
- 状态栏和待结算文本均包含现实联动时间。

## 2026-05-11 全面复查与修复

### 本轮新增发现

- `去冥土追魂所在医院治疗伤势` 会先命中“医院”地点移动，若当前位置无法直达医院，就被地图规则拦截，导致没有治疗。
- 玩家说明书示例 `从校门前往第七学区公共广场` 缺少默认地图边，第一轮行动容易被路线拦截。
- `矿泉水售卖机旁打听传闻` 这类普通调查会被“矿泉水”商品词误识别为购买，造成误扣学都币。
- `拿/拿走/抢走` 的购买判定过宽，可能和抢夺、伪造资源转移意图互相污染。
- 点名 PVP 只按名字匹配，没有先判断双方本轮是否在同一地点或可合法会合，存在隔空殴打、隔空抢夺的风险。

### 本轮修复

- 自我治疗优先于地图移动处理；护送他人去医院仍不会扣自己的治疗费。
- 默认地图加入 `school_gate -> plaza` 边，校门可以直接前往第七学区公共广场。
- 购买意图收紧：商品名不再单独触发购买，必须有购买、拿取、领取、兑换、补给、吃饭、点餐、付款等明确动作。
- 抢夺/偷取/单方面声明别人给资源会先进入反作弊或冲突压力逻辑，不再被购买逻辑抢先处理。
- PVP 目标筛选增加同轮位置投影：只有双方当前或本轮合法移动后的地点一致，才会结算对目标的被动伤害和私聊影响。

### 本轮验证

- Python 源码编译：通过。
- AST 解析与 `_conf_schema.json` JSON 解析：通过。
- `git diff --check`：通过，仅有 CRLF 换行提示。
- 默认数据检查：45 个地点、41 个 NPC、23 个商店物品、20 个事件预设；默认地图边和三张地图图片存在。
- 服务层回归通过：私聊绑定后签到、多群私聊歧义、自我治疗、校门到公共广场、多商品购买、背包赠物、合法转账、伪造收款拦截、抢夺不过账、同地被动受伤通知、互殴掉血、异地 PVP 不隔空伤人、售卖机传闻不误购买。

## 2026-05-10 自然语言意图十轮复查

### 本轮检查角度

1. Python 源码编译与导入边界。
2. 默认地图、NPC、商店、事件种子完整性。
3. 临时 SQLite 初始化、默认世界写入、后台快照。
4. 地图移动目标抽取，尤其“从 A 前往 B”不能误选起点。
5. 购买意图识别，避免“水穗机构”“商品传闻”“餐厅找人”等文本误扣学都币。
6. 当前地点提及，避免“在医院治疗”“在食堂吃饭”被地点识别吞掉真实动作。
7. 治疗、休息、购买、能力使用、高风险调查的优先级组合。
8. 私聊绑定、签到、玩家前端数据隔离和管理员改密。
9. Web 后台 XSS、API JSON、请求体和权限边界复查。
10. 安装目录同步前的 `git diff --check` 与最终回归。

### 本轮新增修复

- 群命令 payload 解析会先剥离 AstrBot 唤醒前缀，避免 `@bot 行动 ...` 把前缀一起塞进行动文本。
- 行动目标如果只是玩家当前位置，不再阻断后续动作判定；`在医院治疗`、`在食堂吃饭`、`在校门休息` 能进入对应分支。
- 购买识别改为“明确购买/拿取/兑换/补给动作”或明确餐饮、补水、交通动作才触发。
- 移除单纯商品名、地点词、传闻描述导致的误购买，例如“水穗机构传闻”“矿泉水售卖机旁的传闻”“餐厅找人”“食堂找人”。
- 购买选品加入类别优先级：吃饭优先便当/套餐，补水优先水/饮料，医疗与资料卡只有明确拿取或兑换时才购买。
- `使用便携绷带包扎伤口` 不再被误判为购买新绷带，避免背包使用类表达被扣钱。

### 本轮验证

- `py_compile` 使用临时 `PYTHONPYCACHEPREFIX` 通过，规避 Windows 旧 `__pycache__` 写入锁误报。
- 默认数据完整性通过：地点 45、NPC 18、商店 17、事件 20、事件种子 22、反作弊词 17。
- 行动流通过：校门前往校内资料室、购买矿泉水、吃饭购买便当、调查水穗机构被地图拦截、调查幻想御手/滞空回线走核心机密外围提示。
- 私聊绑定与签到链路通过。
- 后台快照通过：地图、边、NPC、商店、事件、世界书、provider 信息均可返回。
- 轮次不变量通过：同轮只能提交一次，结算只处理待处理行动，空轮推进不重复发个人结果。
- 自然语言购买边界通过：`食堂找人`、`餐厅打听传闻`、`矿泉水售卖机旁的传闻`、`查看书库资料卡传闻` 均不会扣钱。

## 私聊与群聊上下文排查

### 检查范围

- 群聊创建角色、群聊签到、私聊签到、私聊绑定。
- 数据库 `worlds`、`characters`、`history` 的实际记录。
- AstrBot 消息事件中的群聊/私聊类型与 `group_id`、`session_id` 差异。

### 复现现象

- 玩家在群 `710187710` 创建并审核通过角色卡。
- 群聊 `签到` 可正常获得学都币，并写入私密历史。
- 同一 QQ 在私聊发送 `签到` 时，插件提示未创建角色。

### 数据证据

- `characters` 表中存在 `group_id=710187710`、`qq_id=1556592332` 的角色，状态为 `approved`。
- 该角色已经绑定 `private_origin=napcat:FriendMessage:1556592332`。
- `history` 表已有该角色的群世界签到记录。

### 根因

私聊命令也会命中通用 `@filter.command("签到")` 处理器。旧版 `_group_id()` 在拿不到真实群号时，会继续读取 `message_obj.session_id`；但私聊事件的 `session_id` 是用户会话 ID，不是群号。于是私聊 `签到` 实际查询的是 `group_id=1556592332` 一类的私聊会话世界，而角色真实保存在 `group_id=710187710` 下，所以提示未创建角色。

### 修复方向

- `_group_id()` 不再把私聊 `session_id` 当作群号。
- 私聊 `状态`、`地图`、`待结算`、`签到` 通过 QQ 号和 `private_origin` 反查玩家绑定的群世界。
- `创建角色`、`行动`、`世界开启` 仍要求在群聊中使用，避免在私聊误创建独立世界。

## 2026-05-10 继续查补充

### 本轮新增发现

- 购买选品还存在一个短口语边界：`我要一日券` 这类表达会进入购买流程，但旧逻辑会把它落到“随机可买商品”或错误票券上。
- 旅行票别名虽然能识别 `地铁票`、`通勤票`，但需要和普通叙述分开，不能把 `地铁站找人` 之类文本误吸进购买分支。
- 私聊绑定歧义需要更明确：同一 QQ 若在多个群世界里都有已审核角色，私聊不能静默猜世界，必须返回歧义提示。

### 本轮修复

- 购买选择器改成先找明确商品匹配，再按票券候选挑选，避免无关商品兜底误买。
- `一日券`、`地铁票`、`通勤票` 这类口语被归入票券候选，`地铁站找人` 不再误触发购买。
- 私聊绑定保留歧义返回 `-1`，并在前端命令层给出明确提示。
- 私聊绑定后的世界反查优先已审核角色，减少“一个可用角色却被判歧义”的情况。

### 本轮验证

- `py_compile` 通过。
- `compileall` 通过。
- 直接函数回归通过：`我要一日券`、`地铁票`、`买通勤一日券` 可命中票券；`地铁站找人` 不会误进购买。
- 直接动作回归通过：`去医院治疗`、`在医院治疗` 进入治疗分支；`去食堂吃饭`、`在食堂吃饭` 进入吃饭/购买分支；`在校门休息` 进入休息分支。
## 2026-05-12 客户反馈修复复查

### 本轮反馈

- 需要物价调控。
- 休息后精力至少恢复 50 以上，设施或花钱恢复更多。
- 死亡/濒死不掉落能力经验。
- 角色状态增加性别，并继续使用现实北京时间。
- 修复结算输出异常导致不输出或输出占位句的问题。

### 本轮修复

- 新增 `shop_price_multiplier_percent` 全局物价倍率，购买商店商品和程序兜底日用品都会按倍率结算，后台标价保持不变。
- 休息逻辑改为基础恢复精力 55 点以上；宿舍、医院、食堂、家庭餐厅等设施或付费休息会进一步提高恢复，并可小幅恢复生命、水分、饱食度或心情。
- 濒死逻辑不再扣除能力经验；死亡保护卡仍会优先消耗并保留 1 点生命。
- 角色卡、数据库、状态栏、自然语言角色卡整理和 WebUI 均支持 `gender` 性别字段。
- 大模型结算结果增加空文本、坏 JSON、缺失玩家结果和旧占位句兜底；发送层也会为空文本补默认可发送内容。
- 默认死亡保护卡描述更新为“濒死本身不扣经验”，并刷新旧默认商品描述。
- 移动后可继续触发休息/治疗/购买，避免“去宿舍休息”只移动不恢复。

### 本轮验证

- `python -m compileall -q .`：通过。
- `_conf_schema.json` JSON 解析：通过。
- `git diff --check`：通过，仅有 CRLF 换行提示。
- 临时 SQLite 行为回归通过：性别状态栏、200% 物价倍率购买拉面和矿泉水、移动到宿舍后休息恢复精力、濒死不扣能力经验、大模型占位句自动回退到程序结果。

## 2026-05-12 冲突强度与 AI 限制反馈修复

### 本轮反馈

- 客户提到“破甲”实际意思是希望突破 AI 输出过保守的问题，让打架和暴力冲突不再像过家家。
- 需要确认是否是调用模型本身受到限制，并想办法改善呈现效果。

### 本轮处理

- 不做模型越狱或安全限制绕过；改为插件硬规则兜底：程序先结算真实伤害、濒死、治疗压力和警备风险，模型只能润色，不能抹掉硬结果。
- 新增 `conflict_damage_multiplier_percent`，默认 130%，用于放大真实 PvP 冲突伤害，可在配置里调到更高或更低。
- 新增 `conflict_narrative_intensity`，默认 3，用于控制冲突文本强度。强度高时程序模板会明确写疼痛、出血、眩晕、衣物损坏、撤离、送医和警备介入等非露骨后果。
- 主动 PvP 与被动受击结果都追加伤害等级提示，目标玩家未行动时也会收到更明确的私聊影响。
- 大模型提示词增加“非露骨但严肃压迫”的冲突表达要求，避免把已结算伤害写成玩闹。

### 本轮验证

- `_conf_schema.json` JSON 解析：通过。
- `python -m compileall -q core main.py`：通过。

## 2026-05-12 NPC/身份/任务扩展修复

### 本轮反馈

- NPC 交互偶发没有有效反馈。
- 玩家战斗或交互时，路过 NPC 应按性格决定是否参与剧情。
- 玩家/NPC、玩家/玩家好感度需要隐藏，不显示具体数值。
- 身份权限误判，例如常盘台学生被提示不属于学校。
- NPC 地点不合理，例如佐天泪子夜间出现在常盘台，NPC 移动不能无铺垫瞬移。
- 战斗描写需要默认使用能力并更有细节。
- 需要按玩家身份给出任务引导、声望成长和非扣钱惩罚。
- 需要体力、音乐/艺术等更多扩展能力影响场景。

### 本轮修复

- 新增 `任务/今日任务/引导` 指令，群聊和绑定私聊均可使用。
- 状态栏与任务页展示声望等级、体力/社交/学业/艺术经验和当前身份引导。
- 身份权限改为同时读取身份与阵营；常盘台/学舍之园、栅川、某高中、风纪委员、警备员、研究机构分别放行对应合理地点。
- NPC 遭遇数据补齐 `faction`，风纪委员/警备员等 NPC 在冲突场景能按阵营介入。
- NPC 默认数据刷新不再覆盖已有 `location_key`，避免每次初始化把 NPC 拉回出生点造成不连续。
- NPC 按现实时间和身份修正活动范围；旧库里已经处在不合理地点的 NPC 会静默校正，避免公开写出不合理移动。
- 路径移动会在途经地点随机触发传闻、NPC 擦肩或 NPC 按性格反应。
- 玩家找 NPC 时，即使 NPC 不在本地，也会按可联系性、对方主要活动地点、性格和记忆给出有限反馈。
- 好感度数值继续只存在后台关系表，私聊结果不输出具体关系增减。
- 扩展经验开始参与结算：体力略减冲突承伤并增加战斗收益，社交降低互动消耗/提升正向反馈，学业增加课程收益，艺术影响创作/表演类收益。

### 本轮验证

- `python -m compileall -q core main.py`：通过。
- `_conf_schema.json` JSON 解析：通过。
- 临时 SQLite 行为回归通过：任务指令、常盘台身份权限、研究所权限边界、NPC 遭遇阵营、NPC 位置不再被默认种子重置、默认战斗能力细节、被动受击私聊、关系数值隐藏。

## 2026-05-13 背包食物误扣钱修复

### 本轮反馈

- 玩家吃背包里的食物时被当成购买，额外扣除学都币。

### 根因

- 购买判定把“吃/喝 + 商品名”归入生活消费，没有先检查背包库存。
- “背包里的便当”这类文本还可能被地点模糊匹配带到食堂，造成额外移动。

### 本轮修复

- 新增背包使用分支，优先于购买分支执行。
- 明确“背包/包里/已有/随身/拿出/用掉/消耗”等表达时，只消耗背包物品，不扣学都币。
- 背包没有对应物品时只提示数量不足，不会自动购买。
- 背包使用类文本不再参与地点抽取，避免“吃背包便当”被误判为去食堂。

### 本轮验证

- 临时 SQLite 回归通过：背包有 `学生便当` 时，`行动 吃背包里的学生便当` 不扣学都币，库存 -1，饱食/水分恢复生效，位置保持不变。
- 临时 SQLite 回归通过：背包没有对应物品时，只提示“未自动购买”，学都币不变。

## 2026-05-15 背包/购买混合动作与互动预测复查

### 本轮新增发现

- `买矿泉水并吃背包里的学生便当` 会优先进入背包使用分支，导致购买动作被跳过；修复过程中还发现购买器会把“背包里的学生便当”误当成要再次购买的商品。
- `去第七学区商业入口买水放进背包` 里的“背包”会被当成背包来源，且购买/休息/治疗动作在同轮位置预测中被固定为原地，影响玩家到同一地点后的互动、PVP 和偶遇判定。
- “买水放进背包”语义上是备货，不应该立即补水；旧购买逻辑会直接把商品效果加到状态上。

### 本轮修复

- 将背包语义拆成“来源”和“目的地”：`背包里/从背包/拿出/用掉/消耗` 才按背包物品使用，`放进背包/放到包里/装进背包` 只按购买后入库。
- 购买结算前会剥离“从背包使用某物”的片段，避免混合动作里把已使用的背包物品再次购买。
- 支持同一行动内移动、购买和背包使用叠加结算：例如“去商业入口买水并吃背包便当”会先移动，再分别处理购买和背包使用。
- 明确“放进背包/放到包里”时，商品只入库，不即时恢复水分、饱食度、生命或精力。
- 玩家互动和 PVP 的同轮位置预测改为先看地图移动，避免“去商场买水/去医院治疗/去宿舍休息”这类动作被错误留在原地。

### 本轮验证

- 临时 SQLite 回归通过：`买矿泉水并吃背包里的学生便当` 只购买矿泉水，学生便当从背包扣 1，学都币只扣矿泉水价格。
- 临时 SQLite 回归通过：`去第七学区商业入口买水放进背包` 会移动到商业入口，矿泉水入库，学都币扣除，但不即时补水。
- 临时 SQLite 回归通过：纯 `吃背包里的学生便当` 不扣学都币，不触发购买，库存正确减少。
- 临时 SQLite 互动回归通过：主动转账会过账；伪造“对方给了我钱”会被反作弊拦截；抢夺学都币不会转移资源，会按冲突伤害和被动私聊处理。
- 临时 SQLite 互动回归通过：双方移动到 `Seventh Mist` 后，一方攻击另一方、一方购买矿泉水，仍能按同地交互和被动受击结算。

## 2026-05-16 群聊管理员审核角色卡

### 本轮需求

- 允许管理员不打开 WebUI，直接在群聊里批准或拒绝某些人的角色卡。

### 本轮实现

- 新增群聊管理员指令 `待审核角色`，显示当前群待审核角色卡摘要。
- 新增群聊管理员指令 `审核角色 游戏名或QQ 通过`，可按 QQ、完整游戏名、显示名或唯一模糊游戏名批准角色卡。
- 支持 `审核角色 游戏名或QQ 拒绝`、`审核角色 游戏名或QQ 退回`，分别改为已拒绝或待审核。
- 支持快捷写法 `通过角色 游戏名或QQ`、`批准角色 游戏名或QQ`、`拒绝角色 游戏名或QQ`。
- 审核目标匹配到多个角色时不会猜测，会提示管理员改用 QQ 号或完整游戏名。
- 插件版本号更新为 `0.4.3`。

### 本轮验证

- `python -m compileall -q core main.py`：通过。
- `_conf_schema.json` JSON 解析：通过。
- `git diff --check`：通过，仅有 CRLF 换行提示。
- 临时 SQLite 回归通过：创建 3 个待审核角色，列表可读取；按游戏名通过、按 QQ 拒绝、按模糊唯一名退回均能正确更新状态；不存在角色会返回明确错误。

## 2026-05-19 公共事件改为参与玩家私聊

### 本轮需求

- 大事件和公共事件默认不再发群。
- 只给已参与游戏的玩家私聊发送事件摘要，不参与/未审核角色不发送。
- 群聊保留注册、角色审核、查询、行动提交等指令用途。

### 本轮修复

- 新增 `send_public_summary_to_group` 配置，默认关闭；需要临时恢复群聊剧情公告时可手动开启。
- 新增 `include_public_summary_in_private` 配置，默认开启；每轮公共摘要会合并进所有已审核玩家私聊。
- 结算时会补齐所有已审核玩家作为私聊接收者，本轮没行动的玩家也能收到公共事件/新闻摘要。
- 未审核角色不会进入公共事件私聊名单。
- 大模型提示词、README、配置说明和 WebUI 文案从“群公告”调整为“公共摘要/私聊发送”。
- 修复无行动玩家空私聊结果被旁白兜底误写成“文本生成异常”的问题。
- 插件版本号更新为 `0.4.4`。

### 本轮验证

- 临时 SQLite 回归通过：两个已审核玩家、一个未审核玩家，强制事件结算后只生成两个已审核玩家私聊；其中未行动玩家收到公共摘要和“本轮未行动”提示。
- `python -X utf8` 回归通过：`PRIVATE_EVENT_DELIVERY_OK ['1001', '1002']`。

## 2026-06-18 小地图上下文与消极回复优化

### 本轮反馈

- 客户希望给 AI 加一个更容易记忆状态的小地图，帮助它理解位置和路线。
- 客户希望文游但不要太保守，避免动不动出现消极回复或程序式拒绝。

### 本轮修复

- 结算结果新增 `map_context`，包含地点列表、路线边、已审核玩家位置、NPC 位置和最大移动步数，供剧情模型直接理解小地图。
- 个人结果新增“位置小地图”文本，会显示当前位置、当轮路线、相邻可去地点和附近可见角色。
- 普通行动兜底文案改成更像文游旁白，不再写“未识别到特殊奖励、交易或伤害”这种程序口吻。
- 旁白提示词新增“小地图规则”和“不要输出消极拒绝”的要求，让 AI 更倾向写观察、伏笔、等待、擦肩和下一轮可继续推进的结果。
- 新增宿舍相关地点别名，宿舍门口、宿舍门前、常盘台宿舍门口等更容易落到 `dorm`。
- 插件版本号更新为 `0.4.5`。

### 本轮验证

- `python -m compileall -q core main.py`：通过。
- `_conf_schema.json` JSON 解析：通过。
- `python -X utf8` 临时回归计划通过，目标是验证 `map_context` 与个人结果小地图输出。
